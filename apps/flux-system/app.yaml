template: .gitops
deployments:
  dev:
    target: dev
    values:
      app: flux-system
      namespace: flux-system
      workspace: flux-system
      repo: cse-labs/demo-fleet
      branch: main
      targets: [ all ]
